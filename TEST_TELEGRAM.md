# Тестирование Telegram интеграции

## Быстрая проверка

### 1. Проверка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
cp .env.local.example .env.local
```

Откройте `.env.local` и вставьте реальные значения:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_GROUP_CHAT_ID=-1001234567890
```

### 2. Запуск приложения

```bash
npm run dev
```

### 3. Проверка через браузер

1. Откройте http://localhost:3000
2. Добавьте товары в корзину
3. Нажмите "Оформить заказ"
4. Заполните все поля формы:
   - Выберите способ оплаты
   - Выберите способ доставки
   - Введите телефон (и адрес, если доставка)
5. Нажмите "Подтвердить заказ"
6. Проверьте Telegram группу - должно прийти уведомление

### 4. Проверка через DevTools

Откройте консоль браузера (F12) и проверьте:
- ✅ Нет ошибок при отправке заказа
- ✅ Запрос к `/api/telegram` выполнен успешно (статус 200)

## Тестирование API напрямую

Если хотите протестировать API отдельно от UI:

### Через curl:

```bash
curl -X POST http://localhost:3000/api/telegram \
  -H "Content-Type: application/json" \
  -d '{
    "orderId": "TEST123",
    "date": "2025-10-28T12:00:00.000Z",
    "totalPrice": 2500,
    "products": [
      {
        "name": "Капучино",
        "size": "medium",
        "sirop": "vanilla",
        "qty": 2,
        "final_price": 1000
      },
      {
        "name": "Круассан",
        "size": "medium",
        "sirop": "none",
        "qty": 1,
        "final_price": 500
      }
    ],
    "orderData": {
      "paymentMethod": "cash",
      "deliveryMethod": "delivery",
      "address": "Astanahub, офис 123",
      "phone": "+7 (777) 123-45-67",
      "comment": "Позвонить за 5 минут"
    }
  }'
```

### Через Postman:

1. Создайте POST запрос на `http://localhost:3000/api/telegram`
2. Установите Headers:
   ```
   Content-Type: application/json
   ```
3. Вставьте в Body (raw JSON):
   ```json
   {
     "orderId": "TEST123",
     "date": "2025-10-28T12:00:00.000Z",
     "totalPrice": 2500,
     "products": [
       {
         "name": "Латте",
         "size": "big",
         "sirop": "caramel",
         "qty": 1,
         "final_price": 1500
       }
     ],
     "orderData": {
       "paymentMethod": "kaspi",
       "deliveryMethod": "pickup",
       "phone": "+7 (777) 999-88-77"
     }
   }
   ```
4. Отправьте запрос
5. Проверьте Telegram группу

## Ожидаемые результаты

### ✅ Успешная отправка:

**Response (200):**
```json
{
  "success": true,
  "message": "Order sent to Telegram"
}
```

**В Telegram группе появится сообщение:**
```
🔔 НОВЫЙ ЗАКАЗ #TEST123

📅 Дата: 28.10.2025 14:00:00

📦 Состав заказа:
1. Латте
   • Размер: Большой
   • Сироп: caramel
   • Количество: 1 шт.
   • Цена: 1500 тг

💰 ИТОГО: 1500 тг

💳 Оплата: Kaspi Pay
🚚 Доставка: Самовывоз
📱 Телефон: +7 (777) 999-88-77

✅ Статус: Ожидает подтверждения
```

### ❌ Ошибки:

**Отсутствуют переменные окружения (500):**
```json
{
  "error": "Telegram configuration missing"
}
```
**Решение:** Проверьте файл `.env.local` и перезапустите сервер

**Неверный токен бота (500):**
```json
{
  "error": "Failed to send message to Telegram"
}
```
**Решение:** Проверьте правильность `TELEGRAM_BOT_TOKEN`

**Неверный Chat ID (500):**
В консоли: `"Bad Request: chat not found"`
**Решение:** Проверьте правильность `TELEGRAM_GROUP_CHAT_ID`

## Checklist перед деплоем

- [ ] Telegram бот создан через @BotFather
- [ ] Бот добавлен в группу как администратор
- [ ] Получен Chat ID группы
- [ ] Переменные окружения настроены в Vercel/хостинге
- [ ] Протестирован тестовый заказ
- [ ] Уведомление успешно приходит в группу
- [ ] Проверена корректность форматирования сообщения
- [ ] `.env.local` добавлен в `.gitignore`

## Production окружение (Vercel)

Для деплоя на Vercel:

1. Откройте настройки проекта в Vercel
2. Перейдите в Settings → Environment Variables
3. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_GROUP_CHAT_ID`
4. Выберите окружение (Production, Preview, Development)
5. Сохраните и сделайте redeploy

## Дополнительно

### Логирование

В консоли сервера Next.js будут отображаться ошибки отправки:
```bash
Error sending message to Telegram: ...
Telegram API Error: { description: '...' }
```

### Отладка

Для детальной отладки можно временно добавить в `/app/api/telegram/route.ts`:

```typescript
console.log('Sending to Telegram:', {
    botToken: botToken ? '***' : 'missing',
    chatId: chatId || 'missing',
    messageLength: message.length
});
```

Не забудьте удалить отладочные логи перед коммитом!

