# 🔄 Схема работы системы заказов с Telegram

## Архитектура системы

```
┌─────────────────┐
│   КЛИЕНТ        │
│   (Browser)     │
└────────┬────────┘
         │ 1. Оформляет заказ
         │
         ▼
┌─────────────────────────────────┐
│   CheckoutForm.tsx              │
│   • Сбор данных заказа          │
│   • Валидация формы             │
│   • onOrderSubmit(orderData)    │
└────────┬────────────────────────┘
         │ 2. Передает данные
         │
         ▼
┌─────────────────────────────────┐
│   Zustand Store                 │
│   (app/context/index.tsx)       │
│                                 │
│   addOrder(orderData) {         │
│     • Создает объект заказа     │
│     • Вызывает Telegram API ────┼──┐
│     • Сохраняет в хранилище     │  │ 3. POST запрос
│     • Очищает корзину           │  │
│   }                             │  │
└─────────────────────────────────┘  │
                                     │
         ┌───────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   API Route                     │
│   /app/api/telegram/route.ts    │
│                                 │
│   • Получает данные заказа      │
│   • Читает env переменные       │
│   • Форматирует сообщение       │
│   • Отправляет в Telegram ──────┼──┐
└─────────────────────────────────┘  │ 4. HTTPS POST
                                     │
         ┌───────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Telegram Bot API              │
│   api.telegram.org/bot...       │
│                                 │
│   • Принимает сообщение         │
│   • Валидирует токен            │
│   • Отправляет в группу ────────┼──┐
└─────────────────────────────────┘  │ 5. Доставка
                                     │
         ┌───────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Telegram Группа               │
│                                 │
│   🔔 НОВЫЙ ЗАКАЗ #123           │
│   📅 Дата: 28.10.2025           │
│   📦 Состав заказа: ...         │
│   💰 ИТОГО: 2500 тг             │
│                                 │
│   [Менеджеры видят заказ]       │
└─────────────────────────────────┘
```

## Поток данных

### 1. Клиент оформляет заказ

**Данные формы:**
```typescript
{
  paymentMethod: 'cash' | 'card' | 'kaspi',
  deliveryMethod: 'pickup' | 'delivery',
  address?: string,
  phone: string,
  comment?: string
}
```

### 2. Создание объекта заказа

**Структура заказа:**
```typescript
{
  id: "1730123456",
  date: "2025-10-28T12:30:00.000Z",
  totalPrice: 2500,
  products: [
    {
      name: "Капучино",
      size: "medium",
      sirop: "vanilla",
      qty: 2,
      final_price: 1000
    }
  ],
  orderData: { /* данные формы */ },
  status: 'pending'
}
```

### 3. API запрос к Telegram

**Endpoint:** `POST /api/telegram`

**Request Body:**
```json
{
  "orderId": "1730123456",
  "date": "2025-10-28T12:30:00.000Z",
  "totalPrice": 2500,
  "products": [...],
  "orderData": {...}
}
```

### 4. Форматирование сообщения

**Функция:** `formatOrderMessage()`

Конвертирует JSON → HTML-форматированное сообщение с эмодзи

### 5. Отправка в Telegram

**Telegram API URL:**
```
https://api.telegram.org/bot{TOKEN}/sendMessage
```

**Параметры:**
- `chat_id`: ID группы
- `text`: Форматированное сообщение
- `parse_mode`: 'HTML'

## Обработка ошибок

```
┌────────────────┐
│  API вызов     │
└────────┬───────┘
         │
         ├─ ✅ Успех (200)
         │  └─> Заказ в Telegram
         │
         ├─ ❌ Ошибка конфигурации (500)
         │  └─> "Telegram configuration missing"
         │
         ├─ ❌ Ошибка Telegram API (500)
         │  └─> "Failed to send message"
         │
         └─ ❌ Network Error
            └─> Перехвачено, заказ сохранен локально
```

**Важно:** Даже если Telegram недоступен, заказ всё равно сохраняется в системе!

## Переменные окружения

```env
# Server-side только (Next.js API Route)
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_GROUP_CHAT_ID=-100123...
```

**Безопасность:**
- ✅ Токены доступны только на сервере
- ✅ Клиент не видит токены
- ✅ API route защищен Next.js
- ✅ .env.local в .gitignore

## Сценарии использования

### Сценарий 1: Успешный заказ
```
Клиент → Форма → Zustand → API → Telegram ✅
                    ↓
              localStorage
                    ↓
            История заказов
```

### Сценарий 2: Telegram недоступен
```
Клиент → Форма → Zustand → API → ❌ Ошибка
                    ↓
              localStorage (продолжает работать)
                    ↓
            История заказов ✅
```

### Сценарий 3: Неверная конфигурация
```
Клиент → Форма → Zustand → API → ❌ 500 Error
                    ↓              (логируется)
              localStorage
                    ↓
            История заказов ✅
```

## Масштабирование

### Текущая реализация
- ✅ Отправка уведомлений в 1 группу
- ✅ Форматированные сообщения с эмодзи
- ✅ Обработка ошибок
- ✅ Логирование

### Возможные улучшения
- 📊 Добавить inline-кнопки для управления заказами
- 🔔 Уведомления клиентам о статусе
- 📈 Статистика заказов
- 🗂️ База данных для хранения заказов
- 👥 Разные группы для разных типов заказов
- 🌐 Webhook для обратной связи от Telegram

## Производительность

**Время отклика:**
- Клиент → API: ~50ms
- API → Telegram: ~200-500ms (зависит от сети)
- **Общее:** < 1 секунда

**Нагрузка:**
- Telegram API: 30 сообщений/секунду (лимит)
- Next.js API Route: без ограничений в dev, масштабируется на Vercel

## Мониторинг

**Логи в консоли сервера:**
```bash
# Успех
✅ Order #123 sent to Telegram

# Ошибки
❌ Error sending message to Telegram: ...
❌ Telegram API Error: { description: '...' }
```

**Проверка статуса:**
- Telegram группа - визуальное подтверждение
- Console браузера - Network tab
- Server logs - Terminal с `npm run dev`

## Checklist деплоя

- [ ] Бот создан и получен токен
- [ ] Бот добавлен в группу как админ
- [ ] Получен Chat ID группы
- [ ] Переменные добавлены в Vercel
- [ ] Протестирован на staging
- [ ] Проверены логи на errors
- [ ] Документация обновлена
- [ ] Team уведомлен о запуске

---

**Система готова к использованию!** 🚀

